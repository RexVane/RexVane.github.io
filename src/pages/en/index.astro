---
import BaseLayout from '@/layouts/BaseLayout.astro';
import { Image } from 'astro:assets';
import { getCollection } from 'astro:content';
import avatar from '@/assets/avatar.jpg';

const posts = (await getCollection('blog', ({ id }) => id.startsWith('en/')))
  .filter((post) => !post.data.draft)
  .sort((a, b) => b.data.pubDate.valueOf() - a.data.pubDate.valueOf());
---

<BaseLayout title="RexVane's Blog" description="RexVane's personal blog: tech, projects and everyday life.">
  <section class="profile-section">
    <div class="profile-card">
      <Image class="avatar" src={avatar} alt="RexVane" width={288} height={288} format="webp" quality={82} loading="eager" />
      <div class="profile-info">
        <h1 class="profile-name">RexVane</h1>
        <div class="profile-badges">
          <span class="badge">📍 China</span>
          <a class="badge badge-link" href="https://github.com/RexVane" target="_blank">GitHub</a>
          <a class="badge badge-link" href="mailto:lilifeng811@qq.com">Email</a>
        </div>
      </div>
    </div>
  </section>

  <section class="about-section">
    <h2 class="section-title">About</h2>
    <p class="about-text">
      I'm an undergraduate studying Computer Science and Technology at Guangdong University of Technology (Class of
      2024), steadily exploring my way forward...
    </p>
  </section>

  <section class="posts-section">
    <h2 class="section-title">Recent</h2>
    <div class="timeline">
      {posts.map((post) => (
        <a class="timeline-item" href={`/en/blog/${post.id.replace('en/', '')}/`}>
          <div class="timeline-date">
            <time datetime={post.data.pubDate.toISOString()}>
              <span class="date-day">{post.data.pubDate.toLocaleDateString('en-US', { month: 'short', day: 'numeric' })}</span>
              <span class="date-time">{post.data.pubDate.toLocaleTimeString('en-US', { hour: '2-digit', minute: '2-digit' })}</span>
            </time>
          </div>
          <div class="timeline-content">
            <span class="timeline-type">{post.data.category}</span>
            <h3 class="timeline-title">{post.data.title}</h3>
            <p class="timeline-desc">{post.data.description}</p>
          </div>
        </a>
      ))}
    </div>
  </section>
</BaseLayout>

<style>
  .profile-section {
    display: flex;
    flex-direction: column;
    align-items: center;
    margin-bottom: 2.5rem;
  }

  .profile-card {
    display: flex;
    align-items: center;
    gap: 1.5rem;
  }

  .avatar {
    width: 9rem;
    height: 9rem;
    border-radius: 0.5rem;
    border: 1px solid rgba(255, 255, 255, 0.1);
    object-fit: cover;
    padding: 0.25rem;
  }

  .profile-info {
    display: flex;
    flex-direction: column;
    gap: 1rem;
  }

  .profile-name {
    font-size: 1.875rem;
    font-weight: 700;
    color: #fff;
    margin: 0;
  }

  .profile-badges {
    display: flex;
    flex-wrap: wrap;
    gap: 0.25rem;
  }

  .badge {
    display: inline-flex;
    align-items: center;
    gap: 0.4rem;
    padding: 0.5rem 0.75rem;
    border-radius: 0.6rem;
    background: rgba(255, 255, 255, 0.04);
    border: 1px solid rgba(255, 255, 255, 0.12);
    backdrop-filter: blur(12px) saturate(1.2);
    box-shadow: inset 0 1px 0 rgba(255, 255, 255, 0.08);
    font-size: 0.8rem;
    color: rgba(224, 224, 224, 0.7);
    font-weight: 500;
    transition: all 0.3s ease;
  }

  .badge-link {
    text-decoration: none;
    cursor: pointer;
  }
  .badge-link:hover {
    border-color: rgba(255, 255, 255, 0.25);
    color: #fff;
    transform: translateY(-1px);
    box-shadow:
      inset 0 1px 0 rgba(255, 255, 255, 0.15),
      0 4px 16px rgba(0, 0, 0, 0.3);
    background: rgba(255, 255, 255, 0.08);
  }

  .about-section {
    margin-bottom: 2.5rem;
  }

  .about-text {
    color: rgba(224, 224, 224, 0.7);
    font-size: 0.95rem;
    line-height: 1.7;
    margin: 0;
  }

  .posts-section {
    margin-top: 0;
  }

  .section-title {
    font-size: 1.3rem;
    font-weight: 600;
    color: #fff;
    margin: 0 0 1.5rem;
  }

  .timeline {
    display: flex;
    flex-direction: column;
    gap: 0.75rem;
    position: relative;
    padding-left: 6rem;
  }

  .timeline::before {
    content: '';
    position: absolute;
    left: 5.2rem;
    top: 0;
    bottom: 0;
    width: 1px;
    background: rgba(255, 255, 255, 0.08);
  }

  .timeline-item {
    display: flex;
    align-items: flex-start;
    text-decoration: none;
    padding: 1rem 1.25rem;
    margin-left: 1rem;
    border-radius: 0.75rem;
    border: 1px solid rgba(255, 255, 255, 0.06);
    background: rgba(255, 255, 255, 0.02);
    backdrop-filter: blur(12px) saturate(1.2);
    position: relative;
    transition: all 0.3s ease;
  }
  .timeline-item:hover {
    border-color: rgba(255, 255, 255, 0.15);
    background: rgba(255, 255, 255, 0.06);
    box-shadow:
      inset 0 1px 0 rgba(255, 255, 255, 0.1),
      0 8px 24px rgba(0, 0, 0, 0.3);
    transform: translateX(2px);
  }

  .timeline-item::before {
    content: '';
    position: absolute;
    left: -1.35rem;
    top: 1.4rem;
    width: 8px;
    height: 8px;
    border-radius: 50%;
    background: rgba(255, 255, 255, 0.2);
    border: 2px solid rgba(255, 255, 255, 0.1);
  }
  .timeline-item:hover::before {
    background: rgba(255, 255, 255, 0.5);
  }

  .timeline-date {
    position: absolute;
    left: -5.5rem;
    top: 1rem;
    font-size: 0.75rem;
    color: rgba(224, 224, 224, 0.4);
    white-space: nowrap;
    display: flex;
    flex-direction: column;
    align-items: flex-end;
    line-height: 1.4;
  }

  .date-day {
    display: block;
  }

  .date-time {
    display: block;
    font-size: 0.7rem;
    color: rgba(224, 224, 224, 0.3);
  }

  .timeline-content {
    display: flex;
    flex-direction: column;
    gap: 0.25rem;
  }

  .timeline-type {
    font-size: 0.7rem;
    font-weight: 600;
    text-transform: uppercase;
    letter-spacing: 0.05em;
    color: rgba(123, 164, 219, 0.8);
  }

  .timeline-title {
    font-size: 1.1rem;
    font-weight: 600;
    color: #fff;
    margin: 0;
  }

  .timeline-desc {
    font-size: 0.85rem;
    color: #a1a1aa;
    margin: 0;
  }
</style>
